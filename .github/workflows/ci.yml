# Quality-gate CI — turns the Definition of Done into an enforced gate.
# Templated: fill the placeholder TODO steps once the stack/commands exist
# (Phase 1). Until then this workflow is a no-op scaffold and is safe to keep.

name: ci

on:
  workflow_dispatch:

jobs:
  quality-gate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # --- Toolchain setup (uncomment/replace for your stack) ---
      # - uses: actions/setup-node@v4
      #   with: { node-version: '{{VERSION}}' }

      - name: Install
        run: echo "TODO {{INSTALL_COMMAND}}" # e.g. npm ci

      - name: Lint
        run: echo "TODO {{LINT_COMMAND}}"

      - name: Typecheck
        run: echo "TODO {{TYPECHECK_COMMAND}}" # drop if untyped

      - name: Test
        run: echo "TODO {{TEST_COMMAND}}"

      - name: Build
        run: echo "TODO {{BUILD_COMMAND}}"

      # Post-interview guard: no template placeholder may survive the
      # bootstrap, in ANY tracked file (README.txt, .mdc rules, configs — not
      # just .md/.yml). Enable (uncomment) after the Efes interview fills
      # the skeleton — EFES.md is deleted by then; _template files and
      # historical records (memory bank, ADRs, CHANGELOG), which may quote the
      # placeholder syntax, are exempt.
      # The pattern is written [{][{] so this file doesn't match itself.
      # - name: No placeholders left
      #   run: |
      #     ! git grep -nE '[{][{]' -- ':!*_template*' ':!CHANGELOG.md' ':!docs/memory' ':!docs/architecture/adr'

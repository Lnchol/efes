# Default model for the top-level ("general manager") Codex session — the
# one that decides delegation to .codex/agents/. Fill with the current
# flagship model id (check `codex models list`); not guessed here since it
# may not match what's available on your account/CLI version.
model = "{{CODEX_FLAGSHIP_MODEL}}"
